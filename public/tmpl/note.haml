%script{ :type => "text/x-jsrender",:id => "note-tmpl"}
  .note
    .meta
      .top
        .date-created {{:created_at}}
        .delnote
          %span.rm-icon
            %a.rm-button{ :href => "javascript:void(0);" }
            .clearer
          .clearer
        .clearer
      .user
        .profile-pic
          %img{ :src => "{{: ~userAvatar(user_id, 48) }}", :alt => "{{: ~userName(user_id) }}", :title => "{{: ~userName(user_id) }}" }
      .tags
        .tag.placeholder-tag
          &nbsp;
    .text
      %p {{:html_text}}
    .clearer
